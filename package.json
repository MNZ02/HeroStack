{
  "name": "herostack",
  "private": true,
  "version": "0.0.0",
  "description": "Motion-led hero sections that ship. Each study is its own workspace under web/, showcased through the storefront.",
  "workspaces": [
    "web/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:site": "./scripts/build-site.sh",
    "lint": "turbo run lint",
    "preview": "turbo run preview",
    "clean": "turbo run clean --continue && rm -rf node_modules .turbo"
  },
  "devDependencies": {
    "turbo": "^2.10.7"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@11.12.1"
}
