{
  "name": "deep-sea-jellybot",
  "private": true,
  "version": "0.1.0",
  "description": "Abyssal Medusa JX-7b — a robotic jellyfish rendered as a paper-blueprint mission console, with a three-component exploded view.",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5308 --strictPort",
    "build": "vite build",
    "lint": "oxlint",
    "preview": "vite preview --port 5408 --strictPort",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "oxlint": "^1.71.0",
    "vite": "^6.3.5"
  }
}
