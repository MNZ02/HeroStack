{
  "name": "lucifer",
  "private": true,
  "version": "0.0.0",
  "description": "LUCIFER — The Fall, a scroll-scrubbed descent study.",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5305 --strictPort",
    "frames": "bash scripts/extract-frames.sh",
    "build": "vite build",
    "lint": "oxlint",
    "preview": "vite preview --port 5405 --strictPort",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "oxlint": "^1.71.0",
    "vite": "^8.1.1"
  }
}
